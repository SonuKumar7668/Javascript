const a = "one";
const b = "two";

// Define a function that processes the input and returns a result
export function processData(inputA, inputB) {
    return `Processed data: ${inputA} and ${inputB}`;
}

// Export the constants
export { a, b };